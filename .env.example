# GROQ_API_KEY=
# Required for Phase 2+ (Simplify and later GenAI activities).
# Server-only — never prefix with NEXT_PUBLIC_.
# Get a key at https://console.groq.com/
# Phase 1 (upload → parse → overview) works without this key.
