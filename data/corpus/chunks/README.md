# Hand-curated chunks (Phase 3)

Primary file: [`pilot-corpus.json`](./pilot-corpus.json)

After editing chunks, rebuild embeddings:

```bash
npm run corpus:embed
```

Do not commit unreviewed OCR dumps. Spot-check every excerpt against
`../SOURCES.md` sources before demo day.
